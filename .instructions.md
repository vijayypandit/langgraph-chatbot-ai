# 🧠 Langgraph Chatbot — Project Instructions

---

## 📌 Purpose

This app is a stateful, multi-turn chatbot built with **LangGraph**, **Groq (Llama 3.3 70B)**, and **Streamlit**. Users can ask any query in natural language, and the chatbot returns a streamed response with full conversation history persisted in a local **SQLite database** (`chatbot.db`).

---

## ▶️ Startup

1. 🐍 Activate the project virtual environment:
   - Windows PowerShell: `.\.venv\Scripts\Activate.ps1`
   - Windows Command Prompt: `.\.venv\Scripts\activate.bat`
   - macOS / Linux: `source .venv/bin/activate`

2. 📦 Install dependencies (if not already installed):
   ```bash
   pip install -r requirements.txt
   # Or using uv:
   uv sync
   ```

3. 🔑 Ensure a `.env` file exists in the project root with your Groq API key:
   ```env
   GROQ_API_KEY=your_groq_api_key_here
   ```

4. 🚀 Run the app (recommended database-backed version):
   ```bash
   streamlit run streamlit_frontend_database.py
   ```

   Alternative legacy versions:
   ```bash
   streamlit run streamlit_frontend_streaming.py   # Streaming, memory-only
   streamlit run streamlit_frontend.py              # Static, memory-only
   ```

---

## ⚙️ Environment

- **Python version**: 3.14+
- **Virtual environment**: `./.venv/`
- **Dependencies**: Install from `requirements.txt` or `pyproject.toml`
- **Database**: SQLite3 — auto-created as `chatbot.db` on first run (no setup needed)
- **API**: Requires a valid `GROQ_API_KEY` in `.env`

---

## 📁 Key Files

| File | Description |
|---|---|
| `streamlit_frontend_database.py` | ⭐ **Primary entry point** — Database-backed streaming frontend |
| `langgraph_database_backend.py` | 🗄️ SQLite-backed LangGraph state graph and `retrieve_all_threads()` |
| `streamlit_frontend_streaming.py` | Legacy streaming frontend (memory-only) |
| `langgraph_backend.py` | Legacy backend using `InMemorySaver` |
| `streamlit_frontend.py` | Legacy static (non-streaming) frontend |
| `view_checkpoints.py` | 🛠️ Developer utility to inspect stored threads in `chatbot.db` |
| `main.py` | Basic entry point placeholder |
| `.env` | 🔑 Environment variables (API keys) — **gitignored** |
| `chatbot.db` | 💾 SQLite database file (auto-created at runtime) |

---

## 📐 Workflow Rules

- 📝 Create or update `README.md` whenever app files are changed by you or an agent.
- 📖 Update the theory/explanation content in `README.md` whenever a feature is added or modified.
- ⭐ Keep the primary entrypoint `streamlit_frontend_database.py` stable and only modify when necessary.
- 🔒 Never commit `.env` to version control — it is listed in `.gitignore`.
- 🗄️ The `chatbot.db` SQLite file is auto-managed by `SqliteSaver` — do not manually edit its tables.

---

## 📝 Notes

- This project uses **LangGraph** and **Streamlit** — always activate the virtual environment before running.
- If `streamlit` is not found, ensure venv activation completed and dependencies are installed.
- The SQLite database (`chatbot.db`) is created automatically on first run — no manual setup is required.
- The `check_same_thread=False` flag is used on the SQLite connection because Streamlit re-runs scripts from different threads.
- To inspect stored conversations, run: `python view_checkpoints.py`
